## 硬件环境
- GPU: NVIDIA RTX PRO 6000 Blackwell Server Edition, 98GB VRAM
- 系统盘: 30GB (overlay, 实际可用 ~17GB)
- 数据盘: 150GB (/root/autodl-tmp, 实际已用 ~66GB, 可用 ~85GB)

## 训练环境

### 环境一：PRM 训练环境 (dianjin-prm)
- **位置**: `/root/autodl-tmp/miniconda3/envs/dianjin-prm`
- **模型**: Qwen3-8B (8B参数) + Token分类评分头
- **基座模型路径**: `/root/autodl-tmp/huggingface/models--Qwen--Qwen3-8B/snapshots/b968826d9c46dd6066d109eabc6255188de91218`
- **PRM 训练输出**:
  - 最终检查点: `/root/autodl-tmp/checkpoint/checkpoint-167/`
  - 包含 LoRA adapter (`adapter_model.safetensors`, 231MB) + optimizer/scheduler 状态
- **Python**: 3.12.0
- **PyTorch**: 2.8.0+cu128
- **主要依赖**:
  - transformers 4.56.1
  - peft 0.17.1
  - bitsandbytes 0.49.2
  - accelerate 1.10.1
  - deepspeed 0.17.6
  - datasets 4.0.0
  - flash_attn 2.8.3
  - liger_kernel 0.6.2
- **使用方式**:
  ```bash
  conda activate /root/autodl-tmp/miniconda3/envs/dianjin-prm
  export OMP_NUM_THREADS=1
  cd /root/workspace/qwen-dianjin/DianJin-PRM/src/prm_trainer
  bash train.sh
  ```

### 环境二：TRL+DPO 强化学习环境 (dianjin-dpo)
- **位置**: `/root/autodl-tmp/miniconda3/envs/dianjin-dpo`
- **创建时间**: 2026-06-03
- **策略模型**: Qwen3-8B (与 PRM 基座同源，独立副本)
- **基座模型路径**: `/root/autodl-tmp/huggingface/models--Qwen--Qwen3-8B/snapshots/b968826d9c46dd6066d109eabc6255188de91218`
- **训练框架**: TRL DPOTrainer
- **微调方式**: LoRA (rank=16, alpha=32)，目标模块: q_proj, k_proj, v_proj, o_proj, gate_proj, up_proj, down_proj
- **训练输出路径**: `/root/autodl-tmp/dpo_checkpoint/`
- **Python**: 3.12.0
- **PyTorch**: 2.8.0+cu128
- **主要依赖**:
  - transformers 4.56.1
  - peft 0.17.1
  - accelerate 1.10.1
  - deepspeed 0.17.6
  - datasets 4.0.0
  - trl 0.23.0
  - flash_attn 2.8.3
  - bitsandbytes 0.49.2
- **使用方式**:
  ```bash
  conda activate /root/autodl-tmp/miniconda3/envs/dianjin-dpo
  export OMP_NUM_THREADS=1
  cd /root/workspace/qwen-dianjin/DianJin-PRM/src/dpo_trainer
  bash run_dpo.sh
  ```

### 环境隔离说明
- PRM 环境 (`dianjin-prm`) 和 DPO 环境 (`dianjin-dpo`) 是完全独立的两个 conda 环境
- PRM 模型检查点仅存在于 `/root/autodl-tmp/checkpoint/`，不会被复制到 DPO 训练目录
- 两个环境不共享 pip 包，不使用 `--clone` 创建
- DPO 训练只读取基座模型和偏好数据，不接触 PRM 模型权重

## 数据集

### PRM 训练/验证/测试集
- **路径**: `DianJin-PRM/src/data/` 目录下
- **实际数据量**（2026-06-01 实测）:
  - 训练集: `train/` 文件夹, 310 个 JSONL 文件
  - 验证集: `validate/` 文件夹, 80 个 JSONL 文件
  - 测试集: `test/` 文件夹, 10 个 JSONL 文件
- **每条 PRM 样本包含**: question, knowledge_items, steps(Step 1/2/3), final_answer, step_labels, trajectory_label
- **样本 token 长度范围**: 1809-2625 tokens

### DPO 偏好训练数据
- **数据来源**: CFLUE 金融问答数据集
- **原始数据**: `/root/autodl-tmp/data/tongyi_dianjin/CFLUE/knowledge/train.json` (30,907 条)
- **DPO 训练数据**: `DianJin-PRM/src/dpo_trainer/data/dpo_train.jsonl`
- **DPO 验证数据**: `DianJin-PRM/src/dpo_trainer/data/dpo_test.jsonl`
- **每条 DPO 样本包含**: `prompt`, `chosen`, `rejected`
- **生成方式**: 基于 ground truth 答案和错误答案构造推理偏好对
- **数据预处理脚本**: `DianJin-PRM/src/dpo_trainer/prepare_dpo_data.py`

### （已废弃）GRPO 数据
> GRPO 方法已按 2026-06-03 实验方案更新替换为 DPO。原 GRPO 数据 (`train.parquet` / `test.parquet`) 保留在 `DianJin-PRM/src/output/` 目录中作为历史参考，不再使用。
