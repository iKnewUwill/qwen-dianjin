## 框架：基于LLM微调的银行金融财务推理优化方案

---

### 一、技术思路总览

```
┌─────────────────────────────────────────────────────────────┐
│                     三阶段递进训练架构                         │
├────────────────┬──────────────────┬─────────────────────────┤
│   阶段一        │      阶段二       │       阶段三            │
│  过程奖励模型   │   强化学习数据     │   策略模型强化训练       │
│  训练(PRM)     │   准备            │   (GRPO)               │
├────────────────┼──────────────────┼─────────────────────────┤
│  Qwen3-8B #1   │  高质量金融推理    │   Qwen3-8B #2          │
│       ↓        │  验证数据集        │        ↓               │
│  二分类评分头   │       ↓           │  PRM步骤奖励            │
│  (0/1判断)     │  提示集+LLM验证器  │  + LLM双重验证          │
└────────────────┴──────────────────┴─────────────────────────┘
```

**核心逻辑**：大模型自动合成步骤级二元标注数据 → 训练Token分类式PRM → 为GRPO策略模型提供密集的二分类过程奖励 → 结合LLM双重验证实现结果监督 → 实现金融推理能力的精细优化。

---

### 二、阶段一：过程奖励模型训练

#### 2.1 数据构建流程（⭐核心创新点）

| 步骤 | 内容 | 技术说明 |
|:----:|------|------|
| 1 | 获取上市企业历史财务数据 | 从Wind/Choice提取审计后财报，确保数据质量 |
| 2 | 235B大模型生成财务分析问题 | 多类型问题：比率计算、趋势分析、归因推理、现金流结构分析 |
| 3 | 生成正误推理及答案 | **50%正确 + 50%包含步骤/结论错误**；错误类型：数值提取错误、公式误用、逻辑归因偏差、正负号处理错误 |
| 4 | 构建**LLM双重验证器**：对正确的推理结论，调用235B大模型做二次验证 |

**LLM双重验证机制（⭐核心创新点）**：

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│  模型生成     │     │  验证者1     │     │  验证者2     │
│  推理结论     │────▶│  (235B模型)  │────▶│  (235B模型)  │
│              │     │  独立推理     │     │  二次验证     │
└──────────────┘     └──────────────┘     └──────────────┘
                            │                      │
                            ▼                      ▼
                    如果两次推理结论一致 → 结论正确，样本采用
                    如果两次推理结论不一致 → 结论存疑，样本不采用
```
| 5 | 步骤划分与二元标注 | 按 `<extra_0>` 分隔步骤，235B大模型逐步骤判断正确性，**标签仅为0或1** |
| 6 | 生成训练样本 | 组装为`[问题+知识项+步骤+标签]`标准格式 |

**数据格式示例**：
```
question: 请分析该公司2024年ROE下降的核心驱动因素
knowledge_items: ROE=8.5%, 净利率=12.3%, 权益乘数=1.8, ...
steps: <extra_0>步骤1: 计算杜邦分解...<extra_0>步骤2: 分析净利率变动...<extra_0>...
step_labels: [1, 0, 1, 1]
final_answer: ROE下降主要受净利率下滑影响
trajectory_label: 1
```

> **🔔 创新点**：利用235B大模型自动合成步骤级二元标注数据，突破金融领域人工标注成本高、专业门槛高的双重瓶颈。标注简化为0/1判断，降低大模型标注的模糊性，提升标签一致性。
> **🔔 创新点**：采用LLM双重验证替代传统规则验证或人工验证，解决金融归因题结论不唯一、难以自动判定的难题。两次独立推理的结论一致性作为可靠性指标。


**重难点标注**：
- ⚠️ **难点1**：错误类型的系统化设计。需模拟真实金融业务场景的典型错误，而非随机生成。
- ⚠️ **难点2**：0/1二元标注的边界界定。需在Prompt中明确“部分正确算0还是1”的规则，通过多轮迭代和抽样校验确保一致性。

#### 2.2 模型训练方案

| 要素 | 方案 |
|------|------|
| **基座模型** | Qwen3-8B |
| **架构类型** | Token分类（非因果语言模型） |
| **评分头设计** | 在Qwen3 backbone之上添加2层MLP：`hidden_states → Linear → ReLU → Linear → 2分类logits` |
| **特殊标记** | `<extra_0>` 分隔步骤，`<extra_1>` 标记结束位置 |
| **损失函数** | 仅对 `<extra_0>` 和 `<extra_1>` 位置计算CrossEntropyLoss，其余位置忽略 |
| **微调方式** | LoRA（rank=16, alpha=32），仅训练0.75%参数 |
| **LoRA目标模块** | q_proj, k_proj, v_proj, o_proj, gate_proj, up_proj, down_proj |
| **完全训练模块** | score评分头（2层MLP） |
| **优化器** | 8-bit AdamW (paged)，学习率2e-5，余弦调度，50步预热 |
| **精度** | bf16 |
| **分布式策略** | DeepSpeed ZeRO-2 + CPU Offload |
| **最大序列长度** | 3072 tokens |
| **有效批次大小** | 8 |

> **🔔 创新点**：采用Token分类架构，仅在步骤分隔标记位置计算损失，使模型专注于步骤级质量判断而非文本生成。LoRA仅训练0.75%参数，极大降低训练成本。

**重难点标注**：
- ⚠️ **难点**：特殊标记位置的特征表示能力。`<extra_0>`的隐藏状态需聚合该步骤的全部信息，需验证该设计在金融长推理场景下的有效性。

#### 2.3 效果评估（阶段一）

**评估方式**：无人工标注测试集，采用**大模型验证+统计分析**。

| 评估指标 | 评估方法 | 预期目标 |
|----------|----------|----------|
| **步骤评分准确率** | 留出20%合成数据作为验证集，对比PRM输出与235B模型标签 | 标签一致率>90% |
| **领域适应性** | 在真实金融报告数据上抽样推理，LLM-as-a-judge评估PRM判断合理性 | 可接受的准确率（由235B大模型定性确认） |

---

### 三、阶段二：强化学习数据准备

偏好数据构造流程

| 步骤 | 内容 | 技术说明 |
|:----:|------|------|
| 1 | 提取阶段二数据集中的“问题 + 提示” | 确保提示多样性，覆盖比率计算、归因推理、现金流分析等 |
| 2 | 策略模型对每个提示生成多条候选推理 | Qwen3-8B #2 生成 \( K = 4 \) 条完整推理轨迹 |
| 3 | PRM 对每条轨迹逐步骤打分 | 调用阶段一 Fin-PRM，输出每个 `<extra_0>` 位置的 0/1 分类概率，聚合为轨迹级平均分 |
| 4 | LLM 双重验证器对最终结论打分 | 235B 模型两次独立推理，结论一致则 outcome_reward = 1，否则为 0 |
| 5 | 构建偏好对 | 综合得分 = \( \alpha \times \text{process\_score} + \beta \times \text{outcome\_score} \)；从同一提示下的 \( K \) 条轨迹中选取得分最高的作为 “chosen”，得分最低的作为 “rejected” |
| 6 | 格式化偏好数据 | 组装为 TRL DPO 格式：`prompt, chosen, rejected`，其中 `chosen` 和 `rejected` 均包含完整推理链 |
```
Qwen3-8B #2 生成 K 条推理    →    阶段一 PRM 逐步骤打分    →    235B 双重验证（仅验证结论）
    （生成者）                      （步骤评判者）                  （结果评判者）
```

| 角色 | 模型 | 职责 |
|------|------|------|
| **生成候选** | Qwen3-8B #2 | 对每个 prompt 生成 K 条推理轨迹 |
| **步骤评分** | 阶段一 Fin-PRM | 对每条轨迹的每个 `<extra_0>` 输出 0/1 |
| **结论验证** | 235B 大模型 | 两次独立推理，结论一致→1，不一致→0 |

**235B 的两次调用仅用于判断 8B 输出的最终结论是否正确，不参与候选推理的生成。**

**偏好数据格式示例**：
```json
{
  "prompt": "请分析该公司2024年ROE下降的核心驱动因素...",
  "chosen": "<extra_0>步骤1: 计算杜邦分解...<extra_0>步骤2: 分析净利率变动...",
  "rejected": "<extra_0>步骤1: 直接归因于收入下降...<extra_0>步骤2: 忽略权益乘数变动..."
}
```

> **🔔 创新点**：利用 PRM 过程奖励与双重验证结果奖励的加权融合作为偏好排序依据，使偏好信号同时兼顾推理步骤正确性与最终结论可靠性，区别于传统仅依赖单一结果对错的 DPO 数据构造方式。


### 四、阶段三：策略模型偏好对齐训练

#### 4.1  DPO 训练方案

| 要素 | 方案 |
|------|------|
| **算法** | DPO（Direct Preference Optimization） |
| **训练框架** | TRL（Transformer Reinforcement Learning） |
| **Policy 模型** | Qwen3-8B #2 |
| **参考模型** | 冻结的 Qwen3-8B #2 初始权重（训练前 snapshot） |
| **损失函数** | DPO 标准损失：\( \mathcal{L}_{\text{DPO}} = -\mathbb{E}_{(x, y_w, y_l)} \left[ \log \sigma \left( \beta \log \frac{\pi_\theta(y_w \mid x)}{\pi_{\text{ref}}(y_w \mid x)} - \beta \log \frac{\pi_\theta(y_l \mid x)}{\pi_{\text{ref}}(y_l \mid x)} \right) \right] \) |
| **β 参数** | 0.1（控制偏离参考模型的程度） |
| **微调方式** | LoRA（rank=16, alpha=32），与阶段一 PRM 相同的目标模块 |
| **优化器** | 8-bit AdamW (paged)，学习率 5e-6，余弦调度，50 步预热 |
| **精度** | bf16 |
| **分布式策略** | DeepSpeed ZeRO-2 + CPU Offload |
| **有效批次大小** | 16 |
| **最大序列长度** | 3072 tokens |
| **训练轮数** | 1–3 epochs（偏好数据规模约 5000–10000 对） |

> **🔔 创新点**：将 GRPO 的在线奖励搜索范式替换为 DPO 的离线偏好对齐范式，训练流程更简洁稳定，无需维护回放引擎和奖励归一化，同时保留了 PRM + 双重验证在偏好信号构造中的核心价值。

**重难点标注**：
- ⚠️ **难点1**：偏好对的质量控制。若 chosen 与 rejected 得分差距过小，偏好信号弱，训练效果差。需设置得分差阈值（如 > 0.2），过滤低区分度样本对。
- ⚠️ **难点2**：β 参数调优。β 过小导致策略偏离参考模型过远、出现模式崩塌；β 过大导致对齐效果微弱。需在消融实验中对比多组 β 取值。
- ⚠️ **难点3**：DPO 训练轮数。多轮训练易发生 reward hacking，建议 1–2 轮，监控验证集上的 preference accuracy。

#### 4.2 效果评估（阶段三）

| 评估维度 | 具体指标 | 评估方法 | 预期目标 |
|----------|----------|----------|----------|
| **推理准确率** | 最终答案正确率 | 在测试集上对比 DPO 训练前后 | 相对基座提升 10%–15% |
| **推理过程质量** | 步骤级 LLM 评分 | 235B 模型对推理链逐步骤评判 | 平均步骤正确率提升 15% 以上 |
| **偏好对齐程度** | Preference Accuracy | 验证集上模型对 chosen/rejected 的区分准确率 | > 70% |
| **生成多样性** | 不同推理路径数量 | 同一问题多次生成，统计有效推理路径数 | 多样性保持或提升，不出现模式崩塌 |
| **策略稳定性** | 训练 loss 收敛曲线 | 监控 DPO loss 随训练步数变化 | 稳定下降，无明显震荡 |
| **参考偏离度** | 训练后模型与参考模型的 KL 散度 | 在验证集上估算 \(\text{KL}(\pi_\theta \| \pi_{\text{ref}})\) | 保持在 < 5.0 范围内 |

**消融实验调整说明**：阶段五消融实验方案中，涉及 GRPO 动态权重的实验组（实验组 C 和完整方案组）调整为对应 DPO 的偏好构造方式对比——即“单信号排序（仅过程或仅结果）”vs“加权融合排序”，完整方案组对应“加权融合 + β 参数调优”。其余实验组保持不变。

---

### 五、消融实验方案（⭐补充）

#### 5.1 实验目的

验证各核心组件对最终性能的贡献，定位关键成功因素。

#### 5.2 实验设计

| 实验组 | PRM过程奖励 | 结果奖励 | 验证方式 | 目的 |
|:------:|:-----------:|:--------:|:--------:|------|
| **基线组** | ✗ | ✓（单一模型验证） | 单次LLM判断 | 验证无过程奖励时的基础表现 |
| **实验组A** | ✓（静态权重α=0.5） | ✓ | 单次LLM判断 | 验证过程奖励的贡献 |
| **实验组B** | ✗ | ✓ | 双重验证 | 验证双重验证机制的贡献 |
| **实验组C** | ✓（静态权重α=0.5） | ✓ | 双重验证 | 验证两者结合的贡献 |
| **完整方案组** | ✓（动态权重） | ✓ | 双重验证 | 完整方案，验证动态权重的增益 |

#### 5.3 评估指标

| 指标 | 说明 |
|------|------|
| **最终答案准确率** | 各组在测试集上的答案正确率 |
| **推理步骤质量** | 235B模型对各组推理链的步骤级质量评分均值 |
| **训练收敛速度** | 达到相同准确率所需的训练步数 |
| **奖励信号有效性** | 过程奖励与最终结果正确性的相关度 |

#### 5.4 预期结论

| 对比维度 | 预期结论 |
|----------|----------|
| 实验组A vs 基线 | 过程奖励显著提升推理质量和收敛速度 |
| 实验组B vs 基线 | 双重验证提升结果奖励的可靠性 |
| 实验组C vs A/B | 两者结合效果优于单独使用任一机制 |
| 完整方案 vs 实验组C | 动态权重优于静态权重，训练更稳定 |

---

### 六、创新点总结

| 序号 | 创新点 | 核心价值 |
|:----:|--------|----------|
| ① | **大模型合成步骤级二元标注数据** | 突破金融领域标注瓶颈，二元标签降低标注模糊性 |
| ② | **Token分类式PRM架构** | 仅在步骤分隔标记位置计算损失，专注步骤质量判断，LoRA仅训练0.75%参数 |
| ③ | **LLM双重验证机制** | 两次独立推理结论一致性作为结果奖励，解决金融归因题难以自动判定的难题 |
| ④ | **过程奖励+结果奖励融合的强化训练** | 密集反馈+双重验证硬约束，平衡推理质量与答案正确性 |
| ⑤ | **动态权重策略** | 训练前期鼓励探索，后期强化收敛，避免“刷分”与策略崩溃 |

---

### 七、技术难点与风险应对

| 难点 | 风险描述 | 应对方案 |
|------|----------|----------|
| **合成数据质量** | 235B模型生成的0/1标签存在噪声 | Prompt明确标注规则，多轮抽样校验一致性 |
| **PRM架构适配** | 特殊标记位置的特征表示能力不足 | 验证`<extra_0>`的隐藏状态能否有效聚合步骤信息 |
| **GRPO训练稳定性** | 超参数敏感，组大小与KL系数需精细调优 | 小规模验证实验先行，监控KL散度 |
| **奖励权重设计** | 过程/结果奖励比例失衡 | 消融实验中对比多组权重，根据收敛曲线动态调整 |
| **双重验证成本** | 每次验证调用两次235B模型，计算开销大 | 初期仅对最终答案验证，后续可探索缓存机制 |
| **验证者偏差** | 验证者与生成者同源可能产生系统性偏差 | 调整验证者Temperature参数，改写问题表述确保独立性 |

---