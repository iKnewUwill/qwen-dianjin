## 框架：基于LLM微调的银行金融财务推理优化方案

---

### 一、技术思路总览

```
┌─────────────────────────────────────────────────────────────┐
│                     三阶段递进训练架构                         │
├────────────────┬──────────────────┬─────────────────────────┤
│   阶段一        │      阶段二       │       阶段三            │
│  过程奖励模型   │   强化学习数据     │   策略模型强化训练       │
│  训练(PRM)     │   准备            │   (GRPO)               │
├────────────────┼──────────────────┼─────────────────────────┤
│  Qwen3-8B #1   │  高质量金融推理    │   Qwen3-8B #2          │
│       ↓        │  验证数据集        │        ↓               │
│  二分类评分头   │       ↓           │  PRM步骤奖励            │
│  (0/1判断)     │  提示集+LLM验证器  │  + LLM双重验证          │
└────────────────┴──────────────────┴─────────────────────────┘
```

**核心逻辑**：大模型自动合成步骤级二元标注数据 → 训练Token分类式PRM → 为GRPO策略模型提供密集的二分类过程奖励 → 结合LLM双重验证实现结果监督 → 实现金融推理能力的精细优化。

---

### 二、阶段一：过程奖励模型训练

#### 2.1 数据构建流程（⭐核心创新点）

| 步骤 | 内容 | 技术说明 |
|:----:|------|------|
| ① | 获取上市企业历史财务数据 | 从Wind/Choice提取审计后财报，确保数据质量 |
| ② | 235B大模型生成财务分析问题 | 多类型问题：比率计算、趋势分析、归因推理、现金流结构分析 |
| ③ | 生成正误推理及答案 | **50%正确 + 50%包含步骤/结论错误**；错误类型：数值提取错误、公式误用、逻辑归因偏差、正负号处理错误 |
| ④ | 步骤划分与二元标注 | 按 `<extra_0>` 分隔步骤，235B大模型逐步骤判断正确性，**标签仅为0或1** |
| ⑤ | 生成训练样本 | 组装为`[问题+知识项+步骤+标签]`标准格式 |

**数据格式示例**：
```
question: 请分析该公司2024年ROE下降的核心驱动因素
knowledge_items: ROE=8.5%, 净利率=12.3%, 权益乘数=1.8, ...
steps: <extra_0>步骤1: 计算杜邦分解...<extra_0>步骤2: 分析净利率变动...<extra_0>...
step_labels: [1, 0, 1, 1]
final_answer: ROE下降主要受净利率下滑影响
trajectory_label: 1
```

> **🔔 创新点**：利用235B大模型自动合成步骤级二元标注数据，突破金融领域人工标注成本高、专业门槛高的双重瓶颈。标注简化为0/1判断，降低大模型标注的模糊性，提升标签一致性。

**重难点标注**：
- ⚠️ **难点1**：错误类型的系统化设计。需模拟真实金融业务场景的典型错误，而非随机生成。
- ⚠️ **难点2**：0/1二元标注的边界界定。需在Prompt中明确“部分正确算0还是1”的规则，通过多轮迭代和抽样校验确保一致性。

#### 2.2 模型训练方案

| 要素 | 方案 |
|------|------|
| **基座模型** | Qwen3-8B |
| **架构类型** | Token分类（非因果语言模型） |
| **评分头设计** | 在Qwen3 backbone之上添加2层MLP：`hidden_states → Linear → ReLU → Linear → 2分类logits` |
| **特殊标记** | `<extra_0>` 分隔步骤，`<extra_1>` 标记结束位置 |
| **损失函数** | 仅对 `<extra_0>` 和 `<extra_1>` 位置计算CrossEntropyLoss，其余位置忽略 |
| **微调方式** | LoRA（rank=16, alpha=32），仅训练0.75%参数 |
| **LoRA目标模块** | q_proj, k_proj, v_proj, o_proj, gate_proj, up_proj, down_proj |
| **完全训练模块** | score评分头（2层MLP） |
| **优化器** | 8-bit AdamW (paged)，学习率2e-5，余弦调度，50步预热 |
| **精度** | bf16 |
| **分布式策略** | DeepSpeed ZeRO-2 + CPU Offload |
| **最大序列长度** | 3072 tokens |
| **有效批次大小** | 8 |

> **🔔 创新点**：采用Token分类架构，仅在步骤分隔标记位置计算损失，使模型专注于步骤级质量判断而非文本生成。LoRA仅训练0.75%参数，极大降低训练成本。

**重难点标注**：
- ⚠️ **难点**：特殊标记位置的特征表示能力。`<extra_0>`的隐藏状态需聚合该步骤的全部信息，需验证该设计在金融长推理场景下的有效性。

#### 2.3 效果评估（阶段一）

**评估方式**：无人工标注测试集，采用**大模型验证+统计分析**。

| 评估指标 | 评估方法 | 预期目标 |
|----------|----------|----------|
| **步骤评分准确率** | 留出20%合成数据作为验证集，对比PRM输出与235B模型标签 | >90% |
| **正误区分能力** | 统计正确步骤(标签1)与错误步骤(标签0)的PRM输出概率分布 | 两类概率均值差异显著（p<0.01） |
| **推理链级一致性** | 计算PRM对完整推理链的平均得分与`trajectory_label`的一致性 | >85% |
| **领域适应性** | 在真实金融报告数据上抽样推理，LLM-as-a-judge评估PRM判断合理性 | 可接受的准确率（由业务专家定性确认） |

---

### 三、阶段二：强化学习数据准备

| 步骤 | 内容 |
|:----:|------|
| ① | 抽取高质量金融推理数据集中的“问题”部分，构建GRPO训练提示集 |
| ② | 对提示进行语义改写，增加表达多样性 |
| ③ | 构建**LLM双重验证器**：对推理结论，调用235B大模型做二次验证 |

**LLM双重验证机制（⭐核心创新点）**：

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│  模型生成     │     │  验证者1     │     │  验证者2     │
│  推理结论     │────▶│  (235B模型)  │────▶│  (235B模型)  │
│              │     │  独立推理     │     │  二次验证     │
└──────────────┘     └──────────────┘     └──────────────┘
                            │                      │
                            ▼                      ▼
                    如果两次推理结论一致 → 结论正确 (奖励=1)
                    如果两次推理结论不一致 → 结论存疑 (奖励=0)
```

> **🔔 创新点**：采用LLM双重验证替代传统规则验证或人工验证，解决金融归因题结论不唯一、难以自动判定的难题。两次独立推理的结论一致性作为可靠性指标。

**重难点标注**：
- ⚠️ **难点**：验证者的Prompt设计。需确保两次推理的独立性（如调整Temperature参数、改写问题表述），避免验证者与生成者产生系统性偏差。

#### 3.1 效果评估（阶段二）

| 评估指标 | 评估方法 | 预期目标 |
|----------|----------|----------|
| **验证一致性** | 对已知答案的样本，计算双重验证结果与正确答案的一致率 | >90% |
| **验证稳定性** | 同一结论多次验证，统计结果一致性 | 三次验证中至少两次一致的比例>95% |

---

### 四、阶段三：策略模型强化学习训练

#### 4.1 奖励函数设计

```
Total_Reward = α × Process_Reward + β × Outcome_Reward
```

| 奖励类型 | 来源 | 分值 | 作用 |
|----------|------|:----:|------|
| **Process_Reward** | 阶段一PRM，对每步输出0/1分类概率 | 0或1 | 提供密集的中间反馈 |
| **Outcome_Reward** | LLM双重验证器，两次推理一致 | 0或1 | 硬性结果约束 |

**融合方式**：
- **步骤级**：PRM对每个 `<extra_0>` 位置输出该步骤正确概率，取argmax转为0/1
- **轨迹级**：聚合所有步骤得分（平均或求和），与结果奖励加权融合
- **动态权重**：训练初期 `α=0.7, β=0.3`，后期调整为 `α=0.3, β=0.7`

> **🔔 创新点**：过程奖励与结果奖励均为0/1二值信号，统一简洁；双重验证机制解决金融归因题难以自动判定的难题。

#### 4.2 GRPO训练流程（对齐DianJin-Fin-PRM）

| 要素 | 方案 |
|------|------|
| **算法** | GRPO（Group Relative Policy Optimization） |
| **强化学习框架** | VERL（Volcano Engine Reinforcement Learning） |
| **Policy模型** | Qwen3-8B #2 |
| **奖励来源** | Fin-PRM模型打分 + LLM双重验证 |
| **回放引擎** | vLLM（Tensor Parallelism=2） |
| **分布式策略** | FSDP + Ray协调 |
| **优势估计** | 组内归一化（GRPO核心机制） |
| **KL散度约束** | 防止策略偏离参考模型过远 |

**GRPO训练特点**：
- 对同一提示生成多个候选推理轨迹
- 组内归一化计算相对优势，降低奖励分布的方差
- PRM对每条轨迹的每个步骤输出正确性概率，聚合为密集过程奖励

**重难点标注**：
- ⚠️ **难点1**：GRPO超参数调优。组大小、KL系数、学习率等对稳定性影响大，需小规模实验先行验证。
- ⚠️ **难点2**：奖励权重设计。α/β比例需根据训练曲线动态调整，避免过程奖励过强导致“刷分”或结果奖励过强退化为稀疏反馈。
- ⚠️ **难点3**：验证者推理成本。每次结果验证需调用两次235B模型，大规模训练时需评估计算开销。

#### 4.3 效果评估（阶段三）

| 评估维度 | 具体指标 | 评估方法 | 预期目标 |
|----------|----------|----------|----------|
| **推理准确率** | 最终答案正确率 | 在测试集上对比训练前后 | 相对基座提升15%以上 |
| **推理过程质量** | 步骤级LLM评分 | 235B模型对推理链逐步骤评判 | 平均步骤正确率提升20%以上 |
| **推理完整性** | 有效推理链比例 | 排除中途断裂或无意义输出 | >95% |
| **奖励收敛性** | GRPO训练曲线 | 监控平均奖励随训练步数的变化 | 稳定上升且无明显震荡 |
| **策略稳定性** | KL散度变化 | 监控策略模型与参考模型的KL散度 | 保持在合理范围内，不出现策略崩溃 |

---

### 五、消融实验方案（⭐补充）

#### 5.1 实验目的

验证各核心组件对最终性能的贡献，定位关键成功因素。

#### 5.2 实验设计

| 实验组 | PRM过程奖励 | 结果奖励 | 验证方式 | 目的 |
|:------:|:-----------:|:--------:|:--------:|------|
| **基线组** | ✗ | ✓（单一模型验证） | 单次LLM判断 | 验证无过程奖励时的基础表现 |
| **实验组A** | ✓（静态权重α=0.5） | ✓ | 单次LLM判断 | 验证过程奖励的贡献 |
| **实验组B** | ✗ | ✓ | 双重验证 | 验证双重验证机制的贡献 |
| **实验组C** | ✓（静态权重α=0.5） | ✓ | 双重验证 | 验证两者结合的贡献 |
| **完整方案组** | ✓（动态权重） | ✓ | 双重验证 | 完整方案，验证动态权重的增益 |

#### 5.3 评估指标

| 指标 | 说明 |
|------|------|
| **最终答案准确率** | 各组在测试集上的答案正确率 |
| **推理步骤质量** | 235B模型对各组推理链的步骤级质量评分均值 |
| **训练收敛速度** | 达到相同准确率所需的训练步数 |
| **奖励信号有效性** | 过程奖励与最终结果正确性的相关度 |

#### 5.4 预期结论

| 对比维度 | 预期结论 |
|----------|----------|
| 实验组A vs 基线 | 过程奖励显著提升推理质量和收敛速度 |
| 实验组B vs 基线 | 双重验证提升结果奖励的可靠性 |
| 实验组C vs A/B | 两者结合效果优于单独使用任一机制 |
| 完整方案 vs 实验组C | 动态权重优于静态权重，训练更稳定 |

---

### 六、创新点总结

| 序号 | 创新点 | 核心价值 |
|:----:|--------|----------|
| ① | **大模型合成步骤级二元标注数据** | 突破金融领域标注瓶颈，二元标签降低标注模糊性 |
| ② | **Token分类式PRM架构** | 仅在步骤分隔标记位置计算损失，专注步骤质量判断，LoRA仅训练0.75%参数 |
| ③ | **LLM双重验证机制** | 两次独立推理结论一致性作为结果奖励，解决金融归因题难以自动判定的难题 |
| ④ | **过程奖励+结果奖励融合的GRPO训练** | 密集反馈+双重验证硬约束，平衡推理质量与答案正确性 |
| ⑤ | **动态权重策略** | 训练前期鼓励探索，后期强化收敛，避免“刷分”与策略崩溃 |

---

### 七、技术难点与风险应对

| 难点 | 风险描述 | 应对方案 |
|------|----------|----------|
| **合成数据质量** | 235B模型生成的0/1标签存在噪声 | Prompt明确标注规则，多轮抽样校验一致性 |
| **PRM架构适配** | 特殊标记位置的特征表示能力不足 | 验证`<extra_0>`的隐藏状态能否有效聚合步骤信息 |
| **GRPO训练稳定性** | 超参数敏感，组大小与KL系数需精细调优 | 小规模验证实验先行，监控KL散度 |
| **奖励权重设计** | 过程/结果奖励比例失衡 | 消融实验中对比多组权重，根据收敛曲线动态调整 |
| **双重验证成本** | 每次验证调用两次235B模型，计算开销大 | 初期仅对最终答案验证，后续可探索缓存机制 |
| **验证者偏差** | 验证者与生成者同源可能产生系统性偏差 | 调整验证者Temperature参数，改写问题表述确保独立性 |

---